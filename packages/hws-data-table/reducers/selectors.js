export const getColumns = state => state.app.columns;
export const getModel = state => state.app.model;
export const getRows = state => state.app.rows;
export const getUrl = state => state.app.url;

export const getCachedBuilds = state => state.cache.builds;
export const getCachedLength = state => state.cache.length;
